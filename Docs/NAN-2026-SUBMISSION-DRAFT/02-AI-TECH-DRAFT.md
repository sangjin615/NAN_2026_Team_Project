# 미지의 경매장 — AI 활용 기술 문서 초안

> 공식 제출용 PDF 권장 분량: 5~7페이지

## 1. AI 활용 목적

미지의 경매장은 12일 동안 하루 8개, 총 96개 경매 LOT를 사용한다. 각 LOT에는 단순한 이름뿐 아니라 설명, 소문, 세트 힌트, NPC 반응과 시장 맥락이 필요하다. 모든 문구를 고정 작성하면 반복 플레이의 변화가 줄고, 무제한 생성에 맡기면 가격과 보상 같은 게임 규칙이 흔들릴 수 있다.

따라서 다음 목표로 AI를 사용했다.

1. 같은 게임 규칙 안에서 런마다 다른 추론 재료를 제공한다.
2. 반복 작성량을 줄이되 게임의 핵심 판단은 사람이 설계한 엔진에 남긴다.
3. AI 출력의 누락과 규칙 침범을 자동으로 검증한다.
4. API 장애나 형식 실패가 실제 플레이를 막지 않게 한다.

## 2. 설계 원칙: 생성 모델은 표현하고 엔진은 판정한다

| 엔진이 소유하는 값 | 생성 AI가 작성하는 내용 |
|---|---|
| 기준가·실제 가치 | 품목 표시 이름·설명 |
| 등급·품질·수요 계수 | 소문·세트 힌트 |
| 보상·확률·배수 | NPC 반응·경쟁자 말투 |
| 입찰·판매·대출 규칙 | 의뢰 제목·플레이버 문구 |
| 승리·실패·파산 판정 | 감정·경매·결산 표시 문구 |
| 12일 일정과 모든 ID | 유물 설정·엔딩 표현 |

이 경계 덕분에 모델이나 프롬프트가 바뀌어도 경제 규칙과 저장 데이터는 유지된다. 생성 결과에 `basePrice`, `trueValue`, `reward`, `multiplier`, `probability` 같은 금지 필드가 포함되면 검증에서 실패한다.

## 3. 2단계 생성 구조

### 3.1 런 시작 생성

게임 시작 시 한 번 생성한다.

- 12일 시장 흐름과 분위기
- 입력된 세트 ID별 제목, 공통 비밀, 공개 힌트
- 경쟁자별 말투와 첫 등장 문구
- 유물별 표시 이름과 설정
- 성공·실패·파산 엔딩 표현

모델은 엔진이 전달한 세트·경쟁자·유물 ID를 추가하거나 변경할 수 없다.

### 3.2 일일 생성

매일 필요한 콘텐츠를 생성한다.

- 정확히 8개 LOT의 표시 이름, 설명, 소문, 세트 힌트, NPC 반응
- 당일 제공되는 의뢰 ID별 제목과 플레이버 문구
- 감정 안내·성공·주의 문구
- 경매 시작·재입찰·낙찰·패찰 문구
- 하루 결산 요약

현재 날짜가 진행될 때 2일 뒤까지 준비하여 플레이 중 대기를 줄이는 구조다.

### 3.3 실패 처리

```text
엔진 입력 생성
→ 모델 호출
→ JSON·ID·필수 필드·금지 수치 검증
→ 실패 시 낮은 온도로 1회 교정
→ 다시 실패하면 로컬 폴백
→ 검증된 결과만 게임에 공급
```

`[추가 권장]` 최종 PDF에는 위 흐름을 간단한 다이어그램으로 시각화한다.

## 4. VSL, 게임 엔진, AI, Team Loop의 역할

### VSL

씬 목록, 화면 플로우, 좌표와 크기, UI 요소, 사운드와 전환의 골격을 만든다. 최종 VSL 결과는 어댑터와 매핑 파일을 통해 게임 상태에 연결한다.

### 게임 엔진

12일 일정, 96개 LOT, 입찰, 감정, 판매, 의뢰, 대출, 파산, 결산과 유물 경매를 결정론적으로 처리한다.

### 생성 AI

엔진이 확정한 구조와 ID를 바꾸지 않고 플레이어에게 보이는 표현 콘텐츠를 채운다.

### Team Loop와 검증 하네스

생성 결과가 스키마, ID, 개수와 역할 경계를 지키는지 검사하고, 게임 회귀 테스트까지 함께 실행한다. 도구 자체가 게임의 주인공은 아니며, AI 생성물을 실제 게임에 안전하게 연결하기 위한 제작 안전장치다.

## 5. 사용 도구와 활용 내역

| 도구 | 활용 범위 | 최종 제출 전 확인 |
|---|---|---|
| View Spec Lite | 씬·UI·플로우·좌표·사운드 골격 제작 예정 | 최종 ZIP, 버전, 실제 채택 범위 확인 |
| Codex | VSL 대기 중 모듈형 웹 런타임, 데이터 계약, 테스트, 측정 문서 작성 지원 | 사용 기간과 최종 반영 커밋 확인 |
| Claude Code | 별도 프로토타입에서 힌트 태그 수정, 문서·HTML 갱신 및 인수인계 작성 | 최종 런타임에 채택한 변경만 구분해 기재 |
| qwen3:14b / Ollama | API 연결 전 로컬 2단계 JSON 생성과 계약 준수 실험 | Ollama·모델 정확한 버전 캡처 |
| Team Loop | 생성 하네스 실행, 스키마·금지 필드·경로 범위·회귀 검증 | 게임 적용 효과 중심으로 요약 |
| 외부 API | 아직 제공사·모델을 선택하거나 런타임에 연결하지 않음 | 선정 후 모델명, 호출 위치, 지연시간, 비용 입력 |

## 6. 주요 프롬프트 및 계약 방식

장문의 자연어 지시를 매번 전달하는 대신, 고정 규칙을 스킬과 압축 계약으로 분리했다.

핵심 지시 예시는 다음과 같다.

```text
- JSON 객체만 반환한다.
- 입력된 ID를 정확히 보존한다.
- 일일 LOT는 정확히 8개다.
- 가격·실제가치·보상·확률·배수를 생성하지 않는다.
- 필수 표시 문구를 모두 채운다.
- 검증 실패 시 오류 항목만 보정해 한 번 재시도한다.
```

`[추가 권장]` 최종 PDF에는 전체 프롬프트를 싣지 않고, 위 핵심 규칙과 실제 입출력 JSON 일부를 나란히 보여준다.

## 7. 현재 측정 결과

### 생성 계약 개선

| 지표 | 기존 장문 방식 | 스킬·압축 계약 방식 | 현재 해석 |
|---|---:|---:|---|
| 입력 문자 수 | 4,710 | 1,918 | 59.3% 감소 |
| 비교 가능한 입력 토큰 | 2,520 | 1,549 | 38.5% 감소 |
| 엄격 계약 통과 | 0/2 | 3/3 | 소규모 실험에서 신뢰성 개선 |
| 가장 빠른 정상 결과 | 없음 | 26.139초 | 속도 우위는 아직 입증하지 않음 |

### 게임 및 하네스 검증

- `[확정]` 생성 하네스 4/4 통과
- `[확정]` 게임 런타임 테스트 8/8 통과
- `[확정]` 900개 시드에서 86,400 LOT 일정 생성
- `[확정]` 잘못된 12일 일정 0건
- `[확정]` 생성 결과의 엔진 소유 수치 침범 0건
- `[확정]` 검증 대상 경로 범위 위반 0건

### 수치 해석의 한계

현재 실험은 모델별 대규모 통계가 아니다. 따라서 “개발 시간이 59.3% 감소했다”거나 “생성 속도가 59.3% 향상됐다”고 주장하지 않는다. 현재 확인된 효과는 입력량 감소, 형식 준수율 개선, 오류 자동 발견과 재작업 감소다.

## 8. 실제 프로젝트에 미친 영향

### 적용 전

- UI 목업, 기획 문서와 밸런싱 자료가 실제 게임 상태와 분리되어 있었다.
- 생성 AI 요청·응답의 명시적 계약과 실패 처리 방식이 없었다.
- VSL 최종 ZIP을 받기 전에는 시스템 연결 작업을 시작하기 어려웠다.

### 현재

- `[확정]` VSL과 무관하게 12일 진행을 검증할 수 있는 모듈형 런타임이 존재한다.
- `[확정]` 경매·감정·판매·의뢰·경제·대출·결산이 같은 상태 데이터를 공유한다.
- `[확정]` VSL 요소를 교체할 어댑터와 좌표 매핑 계약이 준비되어 있다.
- `[확정]` 실제 API 전 로컬 모델로 생성·검증·재시도 가능성을 확인했다.
- `[확정]` AI가 없어도 폴백으로 12일 진행이 중단되지 않는다.

## 9. 아직 추가하면 좋은 검증

- `[추가 권장]` 서로 다른 시드 3개를 실제로 플레이해 내용 차이를 캡처
- `[추가 권장]` 최종 API 또는 모델별 10회 이상 성공률·중앙 지연시간 측정
- `[추가 권장]` VSL ZIP 적용 전후 수동 수정 요소 수와 작업 시간 비교
- `[추가 권장]` 3~5명 플레이테스트로 재미·이해도·재도전 의사 기록
- `[추가 권장]` AI 콘텐츠가 없는 폴백판과 AI 생성판의 체감 차이 비교
- `[추가 권장]` API 비용과 1런당 예상 생성 비용 기록

## 10. 외부 에셋·오픈소스·라이선스

이 항목은 공식 제출 전에 반드시 완성한다.

| 항목 | 출처 | 라이선스/사용 조건 | AI 생성 여부 | 적용 위치 |
|---|---|---|---|---|
| 경매품 스프라이트 240개 | 팀 제공 `Sprites_Final`, 저장소 `Runtime/assets/items` | `[담당 확인 — 원 제작 도구·권리·배포 조건]` | AI 제작 자료로 전달받음 `[도구명 확인]` | 품목 카드·경매 화면 |
| UI·목업·배경 | 팀 제작 자료 및 VSL 최종 ZIP | `[담당 확인 — 제작자별 출처]` | `[담당 확인]` | 씬별 화면 |
| BGM | 2026-08-03 현재 미연결 | 해당 없음 | 해당 없음 | `audio-map.json`의 `bgm`이 비어 있음 |
| SFX | 2026-08-03 현재 미연결 | 해당 없음 | 해당 없음 | `audio-map.json`의 `sfx`가 비어 있음 |
| JavaScript 패키지 | `Runtime/package.json` | 외부 런타임 패키지 선언 없음, Node.js 내장 모듈만 사용 | 아니오 | 개발 서버·테스트·런타임 |

전체 소스 저장소는 https://github.com/sangjin615/NAN_2026_Team_Project 이다. 2026-08-03 확인 시 비공개 저장소이므로 제출 전 공개 전환 또는 심사 계정 초대가 필요하다.

`[추가 권장]` GitHub에 `THIRD_PARTY_NOTICES.md` 또는 `ASSET-LICENSES.md`를 두고 PDF에는 요약표와 링크만 싣는다.

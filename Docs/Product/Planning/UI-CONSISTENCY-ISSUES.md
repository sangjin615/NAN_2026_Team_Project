# UI 일관성 문제 추적

기능 삭제·보류는 `REMOVED-DEFERRED-TRACKER.md`에서 관리하고, 이 문서는 화면 비율·에셋·글자·레이아웃 품질 문제를 유형별로 누적한다.

## 판정 기준

| 심각도 | 기준 |
|---|---|
| P0 | 진행 불가, 버튼 조작 불가, 주요 정보가 보이지 않음 |
| P1 | 다른 기능으로 오인하거나 잘못된 값·문구를 표시함 |
| P2 | 비율 왜곡, 겹침, 글자 가독성처럼 완성도를 크게 떨어뜨림 |
| P3 | 미세한 정렬·여백·색상 차이 |

## 발견된 문제

| ID | 유형 | 영향 화면·대상 | 심각도 | 상태 | 처리·다음 조치 |
|---|---|---|---|---|---|
| UI-001 | 이미지에 글자 내장 | 빈 저장칸 문구 | P1 | 해결 | 글자 없는 카드와 HTML 문구로 분리하고 `빈 저장칸`으로 통일 |
| UI-002 | 동적 상태와 내장 문구 불일치 | 새 여정 버튼의 `새 게임` / `선택 슬롯 덮어쓰기` | P1 | 해결 | 글자 없는 공통 버튼 프레임과 동적 HTML 문구 사용 |
| UI-003 | 임시 덮개가 원본 실루엣을 파괴 | 저장칸 번호 `01/02/03` | P2 | 해결 | 사각형 덮개를 리본형 동적 배지로 교체 |
| UI-004 | 서로 다른 완성 이미지 혼용 | 새 여정의 뒤로가기·실행 버튼 | P2 | 해결 | 같은 높이·타이포 규격의 양피지/초록 공통 프레임으로 통일 |
| UI-005 | 원본 비율과 무관한 강제 확대 | 저장 카드 배경 `background-size: 100% 100%` | P2 | 부분 해결 | 16:9 주요 크기에서 레이아웃은 통과. 카드 이미지 자체의 미세 왜곡은 무문자 9슬라이스 카드 제작 시 제거 |
| UI-006 | 글자 내장형 완성 버튼 잔존 | 이어하기의 타이틀·이어하기·저장 삭제 | P2 | 해결 | 무문자 양피지/초록/빨강 프레임과 HTML 문구로 전환 |
| UI-007 | 글자 내장형 뒤로가기 이미지 | 결과 화면 `#return-title` | P2 | 해결 | 공통 초록 버튼과 실제 HTML 텍스트로 교체 |
| UI-008 | 동일 컴포넌트 CSS가 여러 위치에서 재정의됨 | 저장 화면 버튼·카드 | P2 | 부분 해결 | 이어하기 버튼의 중복 시각 규칙 제거. 카드·모드 공통화는 시각 회귀 위험 때문에 후속 정리 |
| UI-009 | 9슬라이스 아래 불투명 배경 노출 | 일부 패널·버튼의 검은 모서리 | P2 | 해결 | 공통 9슬라이스 17종의 네 모서리 알파 0 확인, 배경에는 `background-clip` 적용 |
| UI-010 | 화면 높이에 따라 컨트롤과 카드가 겹침 | 저장 화면 하단 버튼·안내 패널 | P1 | 해결 | 1280×720, 1366×768, 1920×1080 통과. 125% 상당 조건의 안내 패널 패딩·테두리 축소 적용 |
| UI-011 | 이미지형 아이콘에 접근 가능한 글자 숨김 | 설정 아이콘 | P3 | 정상 | `aria-label="설정"`이 있으므로 유지 가능 |
| UI-012 | 배경 자체를 강제 비율로 표시 | 타이틀 등 일부 씬 배경 | P2 | 해결 | 씬 배경 17종이 모두 1672×941(16:9)임을 확인하고 16:9 씬 컨테이너와 일치시킴 |
| UI-013 | 의사요소 배경이 씬 기본 배경 뒤로 사라짐 | 술집 하단 전체가 검게 표시됨 | P0 | 해결 | 술집 배경을 씬 자체에 직접 적용하고 `::before` 배경 레이어 비활성화 |
| UI-014 | 빈 상태 메시지의 프레임만 표시됨 | 술집·거래소·상회·조합 하단의 검은 막대·사각형 | P2 | 해결 | `.status:empty`를 숨기고 실제 메시지가 있을 때만 프레임 표시 |
| UI-015 | 내부 상태 코드가 그대로 노출됨 | 일일 결산의 `대출 none` | P1 | 해결 | `변동 없음`·`상환 완료`·`담보 처분` 사용자 문구로 변환 |
| UI-016 | 다음 행동이 불명확한 버튼 문구 | 결산의 `2일차` 등 | P2 | 해결 | `2일차로`, `최종 유물 경매로`처럼 이동 의미를 명시 |
| UI-017 | 배경에 그려진 빈 패널이 과도하게 남음 | 일일 결산 좌우 패널, 실패 결과의 미사용 패널 | P2 | 보류 | 동적 결산 상세를 채우거나 Figma에서 패널 수를 줄인 무문자 배경 재제작 필요 |
| UI-018 | 실패 사유의 명도 대비 부족 | 실패 결과 중앙의 연분홍 문구 | P2 | 해결 | 실패 전용 짙은 적갈색으로 덮어써 양피지 위 대비 확보 |
| UI-019 | 축약된 이동 버튼 문구 | 결과 화면의 `타이틀로` | P2 | 해결 | `타이틀로 돌아가기`로 통일 |
| UI-020 | 한국어 조사 결합 오류 | 카탈로그 설명의 `흔적와`, `관측와` | P1 | 해결 | 조사 결합을 없앤 `테마와 연결된` 문장 구조로 변경 |
| UI-021 | 정상 진행 검수 데이터 부족 | 3일차 승급 실패 이후 4~12일차·성공 결과·유물 경매 | P1 | 진행 중 | 규칙을 우회하지 않는 테스트 픽스처 또는 정상 플레이 자동화로 후반 씬 별도 검수 |
| UI-022 | 기능명이 지나치게 일반적임 | 이어하기 화면의 `실행` 버튼과 안내 | P2 | 해결 | 목업·타이틀 메뉴와 같은 `이어하기`로 통일 |
| UI-023 | 내부 영어 등급명이 사용자 화면에 노출됨 | 카탈로그·경매·의뢰·보유품·유물 경매 | P2 | 해결 | 일반·희귀·영웅·전설 및 하급·중급·상급으로 표시 전용 변환 |

## 새 화면을 받을 때 확인할 항목

1. 화면 해상도와 브라우저 확대 비율을 같이 기록한다.
2. 버튼 글자가 이미지에 박혀 있는지 확인한다.
3. 카드·버튼의 가로세로 비율이 원본과 달라졌는지 확인한다.
4. 9슬라이스 모서리에 검은색 또는 불투명 사각형이 보이는지 확인한다.
5. 클릭 영역과 보이는 이미지 영역이 같은지 확인한다.
6. 빈 상태·선택 상태·비활성 상태에서 문구와 색상이 맞는지 확인한다.
7. 같은 기능의 뒤로가기·확인·삭제 버튼이 화면마다 다른 규격인지 확인한다.

## 공통 개선 원칙

- 글자가 박힌 완성형 이미지는 가능한 한 무문자 프레임과 HTML 텍스트로 분리한다.
- 크기가 변하는 패널과 버튼은 9슬라이스를 사용하고, 단순 `100% 100%` 확대는 시각 검증 후에만 허용한다.
- 임시 사각형 덮개로 원본 에셋을 가리지 않는다.
- 제목 리본·기본 버튼·보조 버튼·위험 버튼·슬롯 배지는 공통 무문자 세트로 관리한다.
- 해결된 문제도 삭제하지 않고 상태를 `해결`로 남겨 재발 여부를 확인한다.

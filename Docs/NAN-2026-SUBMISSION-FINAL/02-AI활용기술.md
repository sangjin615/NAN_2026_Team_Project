# 미지의 경매장 — AI 활용 기술

> 생성 모델은 표현하고 엔진은 판정한다 — 역할 분리, 2단계 생성, 자동 검증과 폴백

## 1. AI 활용 목적

미지의 경매장은 12일 동안 하루 8개, 총 96개 경매 LOT를 사용한다. 각 LOT에는 단순한 이름뿐 아니라 설명, 소문, 세트 힌트, NPC 반응과 시장 맥락이 필요하다. 모든 문구를 고정 작성하면 반복 플레이의 변화가 줄고, 무제한 생성에 맡기면 가격과 보상 같은 게임 규칙이 흔들릴 수 있다.

따라서 다음 목표로 AI를 사용했다.

1. 같은 게임 규칙 안에서 런마다 다른 추론 재료를 제공한다.
2. 반복 작성량을 줄이되 게임의 핵심 판단은 사람이 설계한 엔진에 남긴다.
3. AI 출력의 누락과 규칙 침범을 자동으로 검증한다.
4. API 장애나 형식 실패가 실제 플레이를 막지 않게 한다.

## 2. 설계 원칙: 생성 모델은 표현하고 엔진은 판정한다

| 엔진이 소유하는 값 | 생성 AI가 작성하는 내용 |
|---|---|
| 기준가·실제 가치 | 품목 표시 이름·설명 |
| 등급·품질·수요 계수 | 소문·세트 힌트 |
| 보상·확률·배수 | NPC 반응·경쟁자 말투 |
| 입찰·판매·대출 규칙 | 의뢰 제목·플레이버 문구 |
| 승리·실패·파산 판정 | 감정·경매·결산 표시 문구 |
| 12일 일정과 모든 ID | 유물 설정·엔딩 표현 |

이 경계 덕분에 모델이나 프롬프트가 바뀌어도 경제 규칙과 저장 데이터는 유지된다. 생성 결과에 `basePrice`, `trueValue`, `reward`, `multiplier`, `probability` 같은 금지 필드가 포함되면 검증에서 실패한다.

## 3. 2단계 생성 구조

### 3.1 런 시작 생성

게임 시작 시 한 번 생성한다.

- 12일 시장 흐름과 분위기
- 입력된 세트 ID별 제목, 공통 비밀, 공개 힌트
- 경쟁자별 말투와 첫 등장 문구
- 유물별 표시 이름과 설정
- 성공·실패·파산 엔딩 표현

모델은 엔진이 전달한 세트·경쟁자·유물 ID를 추가하거나 변경할 수 없다.

### 3.2 일일 생성

매일 필요한 콘텐츠를 생성한다.

- 정확히 8개 LOT의 표시 이름, 설명, 소문, 세트 힌트, NPC 반응
- 당일 제공되는 의뢰 ID별 제목과 플레이버 문구
- 감정 안내·성공·주의 문구
- 경매 시작·재입찰·낙찰·패찰 문구
- 하루 결산 요약

현재 날짜가 진행될 때 2일 뒤까지 준비하여 플레이 중 대기를 줄이는 구조다.

### 3.3 실패 처리

```text
엔진 입력 생성
→ 모델 호출
→ JSON·ID·필수 필드·금지 수치 검증
→ 실패 시 낮은 온도로 1회 교정
→ 다시 실패하면 로컬 폴백
→ 검증된 결과만 게임에 공급
```

## 4. VSL, 게임 엔진, AI, Team Loop의 역할

### VSL

씬 목록, 화면 플로우, 좌표와 크기, UI 요소, 사운드와 전환의 골격을 만든다. 최종 VSL 결과는 어댑터와 매핑 파일을 통해 게임 상태에 연결한다.

### 게임 엔진

12일 일정, 96개 LOT, 입찰, 감정, 판매, 의뢰, 대출, 파산, 결산과 유물 경매를 결정론적으로 처리한다.

### 생성 AI

엔진이 확정한 구조와 ID를 바꾸지 않고 플레이어에게 보이는 표현 콘텐츠를 채운다.

### Team Loop와 검증 하네스

생성 결과가 스키마, ID, 개수와 역할 경계를 지키는지 검사하고, 게임 회귀 테스트까지 함께 실행한다. 도구 자체가 게임의 주인공은 아니며, AI 생성물을 실제 게임에 안전하게 연결하기 위한 제작 안전장치다.

## 5. 사용 도구와 활용 내역

| 도구 | 활용 범위 |
|---|---|
| View Spec Lite | 씬·UI·플로우·좌표·사운드 골격 제작 |
| Codex | 모듈형 웹 런타임, 데이터 계약, 테스트, 측정 문서 작성 지원 |
| Claude Code | 힌트 태그 수정, 문서·HTML 갱신 및 인수인계 작성 |
| qwen3:14b / Ollama | 로컬 2단계 JSON 생성과 계약 준수 실험 |
| Team Loop | 생성 하네스 실행, 스키마·금지 필드·경로 범위·회귀 검증 |
| 생성 API / 모델 | ________________________________ |

## 6. 주요 프롬프트 및 계약 방식

장문의 자연어 지시를 매번 전달하는 대신, 고정 규칙을 스킬과 압축 계약으로 분리했다.

핵심 지시 예시는 다음과 같다.

```text
- JSON 객체만 반환한다.
- 입력된 ID를 정확히 보존한다.
- 일일 LOT는 정확히 8개다.
- 가격·실제가치·보상·확률·배수를 생성하지 않는다.
- 필수 표시 문구를 모두 채운다.
- 검증 실패 시 오류 항목만 보정해 한 번 재시도한다.
```

## 7. 측정 결과

### 생성 계약 개선

| 지표 | 기존 장문 방식 | 스킬·압축 계약 방식 | 해석 |
|---|---:|---:|---|
| 입력 문자 수 | 4,710 | 1,918 | 59.3% 감소 |
| 비교 가능한 입력 토큰 | 2,520 | 1,549 | 38.5% 감소 |
| 엄격 계약 통과 | 0/2 | 3/3 | 소규모 실험에서 신뢰성 개선 |
| 가장 빠른 정상 결과 | 없음 | 26.139초 | 속도 우위는 주장하지 않음 |

### 게임 및 하네스 검증

- 생성 하네스 4/4 통과
- 게임 런타임 테스트 8/8 통과
- 900개 시드에서 86,400 LOT 일정 생성
- 잘못된 12일 일정 0건
- 생성 결과의 엔진 소유 수치 침범 0건
- 검증 대상 경로 범위 위반 0건

### 수치 해석의 한계

이 실험은 모델별 대규모 통계가 아니다. 따라서 “개발 시간이 59.3% 감소했다”거나 “생성 속도가 59.3% 향상됐다”고 주장하지 않는다. 확인된 효과는 입력량 감소, 형식 준수율 개선, 오류 자동 발견과 재작업 감소다.

## 8. 실제 프로젝트에 미친 영향

### 적용 전

- UI 목업, 기획 문서와 밸런싱 자료가 실제 게임 상태와 분리되어 있었다.
- 생성 AI 요청·응답의 명시적 계약과 실패 처리 방식이 없었다.
- VSL 최종 결과를 받기 전에는 시스템 연결 작업을 시작하기 어려웠다.

### 현재

- VSL과 무관하게 12일 진행을 검증할 수 있는 모듈형 런타임이 존재한다.
- 경매·감정·판매·의뢰·경제·대출·결산이 같은 상태 데이터를 공유한다.
- VSL 요소를 교체할 어댑터와 좌표 매핑 계약이 준비되어 있다.
- 생성·검증·재시도 경로를 실제 모델로 확인했다.
- AI가 없어도 폴백으로 12일 진행이 중단되지 않는다.

## 9. 외부 에셋·오픈소스·라이선스

| 항목 | 출처 | 라이선스/사용 조건 | AI 생성 여부 | 적용 위치 |
|---|---|---|---|---|
| 경매품 스프라이트 240개 | 팀 제작 | ______________ | ______________ | 품목 카드·경매 화면 |
| UI·목업·배경 | 팀 제작 및 View Spec Lite | ______________ | ______________ | 씬별 화면 |
| BGM | ______________ | ______________ | ______________ | ______________ |
| SFX | ______________ | ______________ | ______________ | ______________ |
| 폰트·아이콘 | ______________ | ______________ | ______________ | ______________ |
| JavaScript 패키지 | 프로젝트 `package.json` | 외부 런타임 패키지 선언 없음, Node.js 내장 모듈만 사용 | 아니오 | 개발 서버·테스트·런타임 |

전체 소스 저장소는 https://github.com/sangjin615/NAN_2026_Team_Project 이다.

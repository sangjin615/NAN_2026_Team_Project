# 설계 결정 — 에이전트 컨텍스트 규약

- 상태: 결정 (즉시 적용)
- 배경: AI 에이전트 세션의 콜드스타트·대형 파일 통읽기·CLI 원문 덤프가 반복 비용을 만듦.
- 선택한 안:
  1. 저장소 루트 `CLAUDE.md`가 구조 지도·명령·하드 룰의 단일 진입점. 세션은 탐색 대신 이 문서부터 읽는다.
  2. Team Loop 조회·조작은 `tools/tl.ps1` 래퍼로 한 줄 요약만 출력한다. 전체 JSON 덤프 금지.
  3. 생성물(`public/auction/index.html`)은 읽지도 수정하지도 않는다.
  4. 태스크 description에 "참고 파일:" 목록을 명시한다. 에이전트는 그 파일만 우선 읽는다.
  5. 채팅 합의는 `docs/decisions/NNN-*.decision.md`로 기록하고, 재논의 대신 문서를 인용한다.
  6. 세션 하나 = 태스크 하나. 긴 세션은 인계 요약 후 분리한다.
- 기록 사항: B01 기준선은 보드 루프 정착 전에 `ce3b9dd`로 직접 반영되었고, `tsk_c9857cf2947cbfcbe2b9`(B01R)의 사후 검증(PASSED)으로 보완했다. 이후 작업은 루프를 우선한다.
- Team Loop 본체 개선 제안(이 저장소 밖, 별도 처리): `list_tasks` brief 모드(id·상태·제목만), 검증 응답의 실패 라인 요약화.
- 결정자: 최재혁
- 결정일: 2026-07-20
- 재검토 조건: 규약이 작업 속도를 유의미하게 저해할 때
